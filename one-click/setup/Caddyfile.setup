:80 {
    @api path /api/*
    handle @api {
        reverse_proxy localhost:8080
    }

    handle {
        rewrite * /wizard.html
        file_server {
            root /opt/paperless-ag/setup
        }
    }
}
